# TODO

## Баги

- [x] **Карта в редакторе терруара перекрывает шапку админки при прокрутке.**
  Leaflet выставляет своим слоям `z-index` 400–1000, а у липкой шапки `.admin-header` только `z-index: 10`.
  Фикс: в `src/pages/admin/terroirs/[id].astro` добавить `isolation: isolate;` в `.polygon-editor__map`: карта получит свой контекст наложения и не вылезет поверх шапки.
